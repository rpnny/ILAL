# Network-only example. There is no active ILAL deployment preset.
ILAL_CHAIN=84532
ILAL_RPC=https://sepolia.base.org

# Supply contract addresses only from an active versioned deployment manifest.
# ILAL_ISSUER=0x...
# ILAL_REGISTRY=0x...
# ILAL_HOOK=0x...
# ILAL_ROUTER=0x...
# ILAL_TREASURY=0x...
# ILAL_POOL_ID=0x...

# Signers are selected explicitly with --keystore, --rpc-account, or --safe.
# Do not store private keys, keystore passwords, authenticated RPC URLs, or API tokens here.
