# Commands require explicit --rpc and --manifest arguments.
# Example shell variable for local use; the CLI does not auto-load this file.
ILAL_RPC=https://sepolia.base.org

# Select a signer with --keystore and --password-file, or --rpc-account.
# Do not store private keys, passwords, authenticated RPC URLs or API tokens here.
