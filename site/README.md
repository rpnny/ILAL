# ILAL Website

Static landing page for the current ILAL protocol demo.

Open directly:

```bash
open /Users/ronny/ilal/site/index.html
```

Or serve locally:

```bash
cd /Users/ronny/ilal/site
python3 -m http.server 4173
```

Then visit `http://localhost:4173`.
