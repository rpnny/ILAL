import test from 'node:test';import assert from 'node:assert/strict';
import {validateDeploymentConfig,prepareDeployment} from './prepare-deployment.mjs';
const a=n=>'0x'+n.toString(16).padStart(40,'0'),h='0x'+'11'.repeat(32);
const c={format:'ilal-mixed-deploy-config-v1',classification:'testnet',chainId:31337,deployer:a(1),create2Factory:a(2),admin:a(3),poolManager:a(4),token0:a(5),token1:a(6),verifierAdapter:a(7),feed0:a(8),feed1:a(9),sequencerFeed:a(0),startNonce:0,initialTick:0,heartbeat0:3600,heartbeat1:3600,usdDeviationBps:100,pairDeviationBps:100,poolOracleDeviationBps:100,sequencerGracePeriod:0,sequencerRequired:false,lowerTick:-100,upperTick:100,mode:'CNF_ONLY',ceremony:'unsafe-development',policy:{cnfIssuer:a(10),credentialType:h,issuerHash:'0',schemaHash:'0',acceptedRoot:'0',jurisdictionRoot:'0',zkPolicyHash:'0',minKycLevel:0,maxGrantTTL:'3600'}};
test('production rejects dev ceremony and implicit feed settings',()=>{assert.throws(()=>validateDeploymentConfig({...c,classification:'production'}),/ceremony/);assert.throws(()=>validateDeploymentConfig({...c,heartbeat0:undefined}),/threshold/);assert.throws(()=>validateDeploymentConfig({...c,mode:'EITHER'}),/approval/);});
test('deployment plan binds exact nonces, Hook flags and one-time routers without broadcast',()=>{const plan=prepareDeployment(c);assert.equal(plan.status,'UNBROADCAST');assert.equal(BigInt(plan.predicted.hook)&0x3fffn,0xaa8n);assert.deepEqual(plan.transactions.map(t=>t.nonce),plan.transactions.map((_,i)=>i));assert.equal(plan.transactions.filter(t=>t.label.includes('Bind')).length,2);assert.equal(plan.transactions.at(-1).to,plan.predicted.policyRegistry);});
