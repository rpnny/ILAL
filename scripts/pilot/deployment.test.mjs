import test from 'node:test';import assert from 'node:assert/strict';
import {preparePilotDeployment} from './prepare-base-sepolia.mjs';
const address=n=>`0x${n.toString(16).padStart(40,'0')}`,hash=`0x${'11'.repeat(32)}`;
const pilot={format:'ilal-issuer-pilot-config-v1',chainId:84532,roles:{deployer:address(1),issuer:address(2),settlementAssetOperator:address(3),liquidityProvider:address(4),institutionA:address(5),institutionB:address(6),executor:address(7)},assets:{issuerStablecoin:{name:'Issuer Stablecoin',symbol:'iUSD',decimals:6,role:'issuer-stablecoin'},settlementCash:{name:'Sandbox Settlement Cash',symbol:'sUSD',decimals:6,role:'sandbox-settlement-cash'}},scenario:{issuerAssetInput:'100000000',settlementCashInput:'70000000',tickLower:-1000,tickUpper:1000,liquidityDelta:'100000000000000'},policy:{mode:'CNF_ONLY',grantTtlSeconds:3600}};
const dependencies={poolManager:address(10),verifierAdapter:address(11),feedIssuerAsset:address(12),feedSettlementCash:address(13),startNonce:9,sourceCommit:'1'.repeat(40),sourceDigest:'2'.repeat(64),sourceDirty:true,poolManagerCodeHash:hash,verifierAdapterCodeHash:hash};
test('pilot plan deploys separately controlled assets before the unified protocol',()=>{const plan=preparePilotDeployment(pilot,dependencies);assert.equal(plan.status,'UNBROADCAST');assert.equal(plan.transactions[0].nonce,9);assert.equal(plan.transactions[0].label,'Deploy issuer-controlled Asset A');assert.equal(plan.transactions[1].label,'Deploy sandbox settlement Asset B');assert.equal(plan.protocolPlan.configuration.admin,pilot.roles.issuer);assert.equal(plan.protocolPlan.configuration.mode,'CNF_ONLY');assert.equal(plan.operationalEvidence.status,'not completed');assert.deepEqual(plan.transactions.map(row=>row.nonce),plan.transactions.map((_,i)=>9+i));});
