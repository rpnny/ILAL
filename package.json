{
  "name": "ilal-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "protocol",
    "cli",
    "sdk"
  ],
  "scripts": {
    "build": "npm run build --workspace @ilalv3/protocol && npm run build --workspace @ilalv3/sdk && npm run build --workspace @ilalv3/cli",
    "test": "npm run build --workspace @ilalv3/protocol && npm run test --workspace @ilalv3/protocol && npm run test --workspace @ilalv3/sdk && npm run test --workspace @ilalv3/cli",
    "package-check": "npm pack --dry-run --workspace @ilalv3/protocol && npm pack --dry-run --workspace @ilalv3/cli && node scripts/package-smoke-test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "brace-expansion": "^2.1.4",
    "underscore": "1.13.8",
    "ws": "^8.21.0"
  }
}
